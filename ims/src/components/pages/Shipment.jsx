import { Box, Typography } from '@mui/material';

const Shipment = () => {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3, fontWeight: 600 }}>
        Shipment
      </Typography>
      <Typography>Shipment management page...</Typography>
    </Box>
  );
};

export default Shipment;
