import React from 'react';
import { Box, Typography } from '@mui/material';

const Transport = () => {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3, fontWeight: 600 }}>
        Transport
      </Typography>
      <Typography>Transport management page...</Typography>
    </Box>
  );
};

export default Transport;