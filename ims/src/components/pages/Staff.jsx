import React from 'react';
import { Box, Typography } from '@mui/material';

const Staff = () => {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3, fontWeight: 600 }}>
        Staff
      </Typography>
      <Typography>Staff management page...</Typography>
    </Box>
  );
};

export default Staff;