import { Box, Typography } from '@mui/material';

const TransportReport = () => {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3, fontWeight: 600 }}>
        Transport Report
      </Typography>
      <Typography>Transport report content...</Typography>
    </Box>
  );
};

export default TransportReport;
