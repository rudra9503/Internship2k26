import { Box, Typography } from '@mui/material';

const Vendors = () => {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 3, fontWeight: 600 }}>
        Vendors
      </Typography>
      <Typography>Vendors management page...</Typography>
    </Box>
  );
};

export default Vendors;
